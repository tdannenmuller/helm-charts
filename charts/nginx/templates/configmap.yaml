{{- if or .Values.config .Values.serverConfig .Values.streamServerConfig }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "nginx.fullname" . }}
  namespace: {{ include "cloudpirates.namespace" . }}
  labels:
    {{- include "nginx.labels" . | nindent 4 }}
  {{- with (include "nginx.annotations" .) }}
  annotations: 
{{- . | indent 4 }}
  {{- end }}
data:
  {{- if .Values.config }}
  nginx.conf: |-
{{ .Values.config | indent 4 }}
  {{- end }}
  {{- if .Values.serverConfig }}
  server-config.conf: |-
{{ .Values.serverConfig | indent 4 }}
  {{- end }}
  {{- if .Values.streamServerConfig }}
  stream-server-config.conf: |-
{{ .Values.streamServerConfig | indent 4 }}
  {{- end }}
{{- end }}