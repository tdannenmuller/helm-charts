apiVersion: v1
kind: Service
metadata:
  name: {{ include "rustfs.fullname" . }}
  namespace: {{ include "cloudpirates.namespace" . }}
  labels:
    {{- include "rustfs.labels" . | nindent 4 }}
  {{- $annotations := merge .Values.service.annotations .Values.commonAnnotations }}
  {{- with $annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      protocol: TCP
      name: api
      targetPort: api
  selector:
    {{- include "rustfs.selectorLabels" . | nindent 4 }}
{{- if eq .Values.deploymentType "statefulset" }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "rustfs.fullname" . }}-headless
  namespace: {{ include "cloudpirates.namespace" . }}
  labels:
    {{- include "rustfs.labels" . | nindent 4 }}
  {{- with $annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  clusterIP: None
  publishNotReadyAddresses: true
  ports:
    - port: {{ .Values.service.port }}
      protocol: TCP
      name: api
      targetPort: api
  selector:
    {{- include "rustfs.selectorLabels" . | nindent 4 }}
{{- end }}
---
{{- if and .Values.config.consoleEnabled .Values.consoleService.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "rustfs.fullname" . }}-console
  namespace: {{ include "cloudpirates.namespace" . }}
  labels:
    {{- include "rustfs.labels" . | nindent 4 }}
    app.kubernetes.io/component: console
  {{- $consoleAnnotations := merge .Values.consoleService.annotations .Values.commonAnnotations }}
  {{- with $consoleAnnotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.consoleService.type }}
  sessionAffinity: ClientIP
  sessionAffinityConfig:
    clientIP:
      timeoutSeconds: {{ .Values.consoleService.sessionAffinityTimeout }}
  ports:
    - port: {{ .Values.consoleService.port }}
      protocol: TCP
      name: console
      targetPort: console
  selector:
    {{- include "rustfs.selectorLabels" . | nindent 4 }}
{{- end }}
