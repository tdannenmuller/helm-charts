apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: {{ include "keycloak.fullname" . }}
  namespace: {{ include "cloudpirates.namespace" . }}
  labels:
    {{- include "keycloak.labels" . | nindent 4 }}
  {{- if .Values.commonAnnotations }}
  annotations:
    {{- include "keycloak.annotations" . | nindent 4 }}
  {{- end }}
spec:
  serviceName: {{ include "keycloak.fullname" . }}-headless
  podManagementPolicy: OrderedReady
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "keycloak.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      {{- $annotations := merge .Values.podAnnotations .Values.commonAnnotations }}
      {{- with $annotations }}
      annotations: {{- toYaml . | nindent 8 }}
      {{- end }}
      labels:
        {{- include "keycloak.labels" . | nindent 8 }}
        {{- with .Values.podLabels }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
    spec:
      serviceAccountName: {{ include "keycloak.serviceAccountName" . }}
      automountServiceAccountToken: {{ .Values.serviceAccount.automountServiceAccountToken }}
{{- with (include "keycloak.imagePullSecrets" .) }}
{{- . | nindent 6 }}
{{- end }}
      securityContext: {{ include "cloudpirates.renderPodSecurityContext" . | nindent 8 }}
      {{- with .Values.shareProcessNamespace }}
      shareProcessNamespace: {{ . }}
      {{- end }}
      initContainers:
        - name: copy-quarkus-lib
          securityContext: {{ include "cloudpirates.renderContainerSecurityContext" . | nindent 12 }}
          image: {{ include "keycloak.image" . }}
          imagePullPolicy: {{ .Values.image.imagePullPolicy }}
          command: ["sh", "-c", "cp -r /opt/keycloak/lib/quarkus/* /shared-quarkus/"]
          volumeMounts:
            - name: keycloak-lib-quarkus
              mountPath: /shared-quarkus
          resources:
            {{- toYaml .Values.initContainers.copyQuarkusLib.resources | nindent 12 }}
        {{- if .Values.postgres.enabled }}
        - name: wait-for-postgres
          securityContext: {{ include "cloudpirates.renderContainerSecurityContext" . | nindent 12 }}
          image: {{ include "keycloak.initContainerImage" (dict "config" .Values.initContainers.waitForPostgres "global" .Values.global) }}
          imagePullPolicy: {{ .Values.initContainers.waitForPostgres.pullPolicy }}
          command: ["sh", "-c", "until pg_isready -h {{ template "postgres.fullname" .Subcharts.postgres }} -p 5432 -U postgres; do echo waiting for database; sleep 2; done;"]
          resources:
            {{- toYaml .Values.initContainers.waitForPostgres.resources | nindent 12 }}
        {{- else if .Values.mariadb.enabled }}
        - name: wait-for-mariadb
          securityContext: {{ include "cloudpirates.renderContainerSecurityContext" . | nindent 12 }}
          image: {{ include "keycloak.initContainerImage" (dict "config" .Values.initContainers.waitForMariadb "global" .Values.global) }}
          imagePullPolicy: {{ .Values.initContainers.waitForMariadb.pullPolicy }}
          command: ["sh", "-c", "until mariadb-admin ping -h {{ template "mariadb.fullname" .Subcharts.mariadb }} -P 3306 --silent; do echo waiting for database; sleep 2; done;"]
          resources:
            {{- toYaml .Values.initContainers.waitForMariadb.resources | nindent 12 }}
        {{- end }}
        {{- if .Values.extraInitContainers }}
          {{- tpl (toYaml .Values.extraInitContainers) . | nindent 8 }}
        {{- end }}
      containers:
        - name: {{ .Chart.Name }}
          securityContext: {{ include "cloudpirates.renderContainerSecurityContext" . | nindent 12 }}
          image: {{ include "keycloak.image" . }}
          imagePullPolicy: {{ .Values.image.imagePullPolicy }}
          command:
            - {{ .Values.image.command }}
          args:
            {{- with .Values.keycloak.extraArgsPrefix }}
            {{- toYaml . | nindent 12 }}
            {{- end }}
            {{- if .Values.keycloak.production }}
            - start
            {{- else }}
            - start-dev
            {{- end }}
            {{- if .Values.realm.import }}
            - --import-realm
            {{- end }}
            {{- if .Values.keycloak.httpEnabled }}
            - --http-enabled=true
            {{- end }}
            {{- if .Values.keycloak.hostname }}
            - --hostname={{ include "keycloak.hostname" . }}
            {{- end }}
            {{- if .Values.keycloak.hostnameAdmin }}
            - --hostname-admin={{ include "keycloak.hostnameAdmin" . }}
            {{- end }}
            - --hostname-strict={{ .Values.keycloak.hostnameStrict }}
            {{- if .Values.keycloak.hostnameBackchannel }}
            - --hostname-backchannel={{ include "keycloak.hostnameBackchannel" . }}
            {{- end }}
            {{- if .Values.keycloak.proxyHeaders }}
            - --proxy-headers={{ .Values.keycloak.proxyHeaders }}
            {{- end }}
            {{- if .Values.keycloak.proxyProtocolEnabled }}
            - --proxy-protocol-enabled={{ .Values.keycloak.proxyProtocolEnabled }}
            {{- end }}
            {{- if .Values.keycloak.proxyTrustedAddresses }}
            - --proxy-trusted-addresses={{ .Values.keycloak.proxyTrustedAddresses }}
            {{- end }}
            {{- if .Values.keycloak.httpPort }}
            - --http-port={{ .Values.keycloak.httpPort }}
            {{- end }}
            {{- if .Values.keycloak.httpsPort }}
            - --https-port={{ .Values.keycloak.httpsPort }}
            {{- end }}
            {{- if and .Values.database.type (not (has .Values.database.type (list "h2-file" "h2-mem" "dev-mem"))) }}
            - --db={{ .Values.database.type }}
            - --db-url={{ include "keycloak.databaseUrl" . }}
            {{- end }}
            {{- if ne .Values.cache.stack "local" }}
            - --cache={{ .Values.cache.stack }}
            {{- end }}
            {{- if .Values.features.enabled }}
            - --features={{ join "," .Values.features.enabled }}
            {{- end }}
            {{- if .Values.features.disabled }}
            - --features-disabled={{ join "," .Values.features.disabled }}
            {{- end }}
            {{- if .Values.metrics.enabled }}
            - --metrics-enabled=true
            {{- end }}
            {{- if or .Values.livenessProbe.enabled .Values.readinessProbe.enabled .Values.startupProbe.enabled }}
            - --health-enabled=true
            {{- end }}
            {{- with .Values.keycloak.extraArgs }}
            {{- toYaml . | nindent 12 }}
            {{- end }}
            {{- if .Values.tls.enabled }}
            - --https-certificate-file={{ .Values.tls.certificateFile }}
            - --https-certificate-key-file={{ .Values.tls.certificateKeyFile }}
            {{- end }}
            {{- if .Values.tls.truststoreEnabled }}
            - --https-trust-store-file={{ .Values.tls.truststoreFile }}
            {{- if .Values.tls.truststorePassword }}
            - --https-trust-store-password={{ .Values.tls.truststorePassword }}
            {{- end }}
            {{- end }}
          env:
            - name: KC_HTTP_RELATIVE_PATH
              value: {{ .Values.keycloak.httpRelativePath | quote | default "/" }}
            - name: KC_BOOTSTRAP_ADMIN_USERNAME
              value: {{ .Values.keycloak.adminUser | quote }}
            - name: KC_BOOTSTRAP_ADMIN_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ include "keycloak.secretName" . }}
                  key: {{ include "keycloak.adminPasswordKey" . }}
            {{- if or .Values.tls.truststorePassword .Values.tls.truststoreExistingSecret }}
            - name: KC_HTTPS_TRUST_STORE_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.tls.truststoreExistingSecret | default (include "keycloak.fullname" .) }}
                  key: {{ .Values.tls.truststoreExistingSecretKey }}
            {{- end }}
            {{- if .Values.database.type }}
            - name: KC_DB
              value: {{ .Values.database.type }}
            {{- end }}
            {{- if .Values.database.schema }}
            - name: KC_DB_SCHEMA
              value: {{ .Values.database.schema }}
            {{- end }}
            {{- if .Values.database.urlProperties }}
            - name: KC_DB_URL_PROPERTIES
              value: {{ .Values.database.urlProperties | quote }}
            {{- end }}
            {{- if or (eq .Values.database.type "postgres") (eq .Values.database.type "mysql") (eq .Values.database.type "mariadb") (eq .Values.database.type "mssql") }}
            - name: KC_DB_USERNAME
              {{- if and (eq .Values.database.type "postgres") .Values.postgres.enabled }}
              value: {{ default "postgres" .Values.postgres.auth.username | quote }}
              {{- else if and (or (eq .Values.database.type "mysql") (eq .Values.database.type "mariadb")) .Values.mariadb.enabled }}
              value: {{ default "root" .Values.mariadb.auth.username | quote }}
              {{- else }}
              valueFrom:
                secretKeyRef:
                  name: {{ include "keycloak.databaseSecretName" . }}
                  key: {{ include "keycloak.databaseUsernameKey" . }}
              {{- end }}
            - name: KC_DB_PASSWORD
              {{- if and (eq .Values.database.type "postgres") .Values.postgres.enabled }}
              valueFrom:
                secretKeyRef:
                  {{- if .Values.postgres.auth.existingSecret }}
                  name: {{ .Values.postgres.auth.existingSecret }}
                  {{- else if .Values.mariadb.auth.existingSecret }}
                  name: {{ .Values.mariadb.auth.existingSecret }}
                  {{- else }}
                  name: {{ template "postgres.fullname" .Subcharts.postgres }}
                  {{- end }}
                  key: postgres-password
              {{- else if and (or (eq .Values.database.type "mysql") (eq .Values.database.type "mariadb")) .Values.mariadb.enabled }}
              valueFrom:
                secretKeyRef:
                  name: {{ template "mariadb.fullname" .Subcharts.mariadb }}
                  key: {{ if .Values.mariadb.auth.username }}mariadb-password{{ else }}mariadb-root-password{{ end }}
              {{- else }}
              valueFrom:
                secretKeyRef:
                  name: {{ include "keycloak.databaseSecretName" . }}
                  key: {{ include "keycloak.databasePasswordKey" . }}
              {{- end }}
            {{- end }}
            {{- with .Values.extraEnvVars }}
{{- tpl (toYaml .) $ | nindent 12 }}
            {{- end }}
          {{- if .Values.extraEnvVarsSecret }}
          envFrom:
            - secretRef:
                name: {{ .Values.extraEnvVarsSecret }}
          {{- end }}
          ports:
            {{- if .Values.keycloak.httpEnabled }}
            - name: http
              containerPort: {{ .Values.keycloak.httpPort }}
              protocol: TCP
            {{- end }}
            {{- if .Values.keycloak.httpsEnabled }}
            - name: https
              containerPort: {{ .Values.keycloak.httpsPort }}
              protocol: TCP
            {{- end }}
            - name: management
              containerPort: {{ .Values.keycloak.managementPort }}
              protocol: TCP
            {{- if .Values.metrics.enabled }}
            - name: {{ .Values.metrics.service.targetPort }}
              containerPort: {{ .Values.metrics.service.port }}
              protocol: TCP
            {{- end }}
          {{- if .Values.livenessProbe.enabled }}
          livenessProbe:
            httpGet:
              path: {{ include "keycloak.livenessProbeUrl" . }}
              port: management
              scheme: {{ .Values.tls.enabled | ternary "HTTPS" "HTTP" }}
            initialDelaySeconds: {{ .Values.livenessProbe.initialDelaySeconds }}
            periodSeconds: {{ .Values.livenessProbe.periodSeconds }}
            timeoutSeconds: {{ .Values.livenessProbe.timeoutSeconds }}
            successThreshold: {{ .Values.livenessProbe.successThreshold }}
            failureThreshold: {{ .Values.livenessProbe.failureThreshold }}
          {{- end }}
          {{- if .Values.readinessProbe.enabled }}
          readinessProbe:
            httpGet:
              path: {{ include "keycloak.readinessProbeUrl" . }}
              port: management
              scheme: {{ .Values.tls.enabled | ternary "HTTPS" "HTTP" }}
            initialDelaySeconds: {{ .Values.readinessProbe.initialDelaySeconds }}
            periodSeconds: {{ .Values.readinessProbe.periodSeconds }}
            timeoutSeconds: {{ .Values.readinessProbe.timeoutSeconds }}
            successThreshold: {{ .Values.readinessProbe.successThreshold }}
            failureThreshold: {{ .Values.readinessProbe.failureThreshold }}
          {{- end }}
          {{- if .Values.startupProbe.enabled }}
          startupProbe:
            httpGet:
              path: {{ include "keycloak.livenessProbeUrl" . }}
              port: management
              scheme: {{ .Values.tls.enabled | ternary "HTTPS" "HTTP" }}
            initialDelaySeconds: {{ .Values.startupProbe.initialDelaySeconds }}
            periodSeconds: {{ .Values.startupProbe.periodSeconds }}
            timeoutSeconds: {{ .Values.startupProbe.timeoutSeconds }}
            successThreshold: {{ .Values.startupProbe.successThreshold }}
            failureThreshold: {{ .Values.startupProbe.failureThreshold }}
          {{- end }}
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
          volumeMounts:
            - name: data
              mountPath: /opt/keycloak/data
            {{- if .Values.cache.configFile }}
            - name: cache-config
              mountPath: /opt/keycloak/conf/cache-ispn.xml
              subPath: cache-ispn.xml
              readOnly: true
            {{- end }}
            - name: tmp
              mountPath: /tmp
            - name: keycloak-work
              mountPath: /opt/keycloak/work
            - name: keycloak-lib-quarkus
              mountPath: /opt/keycloak/lib/quarkus
            {{- if not .Values.preserveThemes }}
            - name: keycloak-themes
              mountPath: /opt/keycloak/themes
            {{- end }}
            {{- if not .Values.preserveProviders }}
            - name: keycloak-providers
              mountPath: /opt/keycloak/providers
            {{- end }}
            {{- if and .Values.realm.import (or .Values.realm.configFile .Values.realm.existingSecret) }}
            - name: realm-config
              mountPath: /opt/keycloak/data/import
              readOnly: true
            {{- end }}
            {{- if .Values.tls.enabled }}
            - name: tls-certs
              mountPath: /opt/keycloak/certs
              readOnly: true
            {{- end }}
            {{- if .Values.tls.truststoreEnabled }}
            - name: tls-truststore
              mountPath: /opt/keycloak/truststore
              readOnly: true
            {{- end }}
            {{- if .Values.extraVolumeMounts }}
              {{- tpl (toYaml .Values.extraVolumeMounts) . | nindent 12 }}
            {{- end }}
        {{- with .Values.extraContainers }}
          {{- tpl (toYaml .) $ | nindent 8 }}
        {{- end }}
      volumes:
        {{- if and .Values.persistence.enabled .Values.persistence.existingClaim }}
        - name: data
          persistentVolumeClaim:
            claimName: {{ .Values.persistence.existingClaim }}
        {{- else if not .Values.persistence.enabled }}
        - name: data
          emptyDir: {}
        {{- end }}
        {{- if .Values.cache.configFile }}
        - name: cache-config
          configMap:
            name: {{ include "keycloak.fullname" . }}-cache
        {{- end }}
        - name: tmp
          emptyDir: {}
        - name: keycloak-work
          emptyDir: {}
        - name: keycloak-lib-quarkus
          emptyDir: {}
        {{- if not .Values.preserveThemes }}
        - name: keycloak-themes
          emptyDir: {}
        {{- end }}
        {{- if not .Values.preserveProviders }}
        - name: keycloak-providers
          emptyDir: {}
        {{- end }}
        {{- if and .Values.realm.import (or .Values.realm.configFile .Values.realm.existingSecret) }}
        - name: realm-config
          secret:
            secretName: {{ default (printf "%s-realm" (include "keycloak.fullname" .)) .Values.realm.existingSecret }}
        {{- end }}
        {{- if .Values.tls.enabled }}
        - name: tls-certs
          secret:
            secretName: {{ include "keycloak.tlsSecretName" . }}
            items:
              - key: tls.crt
                path: tls.crt
              - key: tls.key
                path: tls.key
        {{- end }}
        {{- if .Values.tls.truststoreEnabled }}
        - name: tls-truststore
          secret:
            secretName: {{ .Values.tls.truststoreExistingSecret }}
            items:
              - key: truststore.jks
                path: truststore.jks
        {{- end }}
        {{- if .Values.extraVolumes }}
          {{- tpl (toYaml .Values.extraVolumes) . | nindent 8 }}
        {{- end }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.priorityClassName }}
      priorityClassName: {{ . }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.topologySpreadConstraints }}
      topologySpreadConstraints:
        {{- toYaml . | nindent 8 }}
      {{- end }}
  {{- if and .Values.persistence.enabled (not .Values.persistence.existingClaim) }}
  volumeClaimTemplates:
    - metadata:
        name: data
        labels:
          {{- include "keycloak.labels" . | nindent 10 }}
        {{- $annotations := merge .Values.persistence.annotations .Values.commonAnnotations }}
        {{- with $annotations }}
        annotations:
          {{- toYaml . | nindent 10 }}
        {{- end }}
      spec:
        accessModes:
          {{- range .Values.persistence.accessModes }}
          - {{ . | quote }}
          {{- end }}
        resources:
          requests:
            storage: {{ .Values.persistence.size | quote }}
        {{- if .Values.persistence.storageClass }}
        {{- if (eq "-" .Values.persistence.storageClass) }}
        storageClassName: ""
        {{- else }}
        storageClassName: {{ .Values.persistence.storageClass | quote }}
        {{- end }}
        {{- end }}
  {{- end }}
