{{- if .Values.shardedCluster.enabled }}
{{/* Data Node Services for each shard */}}
{{- range $shardIndex := until (int .Values.shardedCluster.shards) }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "mongodb.fullname" $ }}-shard-{{ $shardIndex }}-headless
  namespace: {{ include "cloudpirates.namespace" $ }}
  labels:
    {{- include "mongodb.labels" $ | nindent 4 }}
    service-type: shard
    shard-index: "{{ $shardIndex }}"
  {{- with (include "mongodb.annotations" $) }}
  annotations:
{{- . | indent 4 }}
  {{- end }}
spec:
  type: ClusterIP
  clusterIP: None
  publishNotReadyAddresses: true
  ports:
    - name: mongodb
      port: 27017
      targetPort: mongodb
      protocol: TCP
    {{- if $.Values.metrics.enabled }}
    - name: http-metrics
      port: 9216
      targetPort: http-metrics
      protocol: TCP
    {{- end }}
  selector:
    {{- include "mongodb.selectorLabels" $ | nindent 4 }}
    service-type: shard
    shard-index: "{{ $shardIndex }}"
{{- end }}

{{/* Arbiter Services for each shard (if enabled) */}}
{{- if gt (int $.Values.shardedCluster.shardsvr.arbiter.replicaCount) 0 }}
{{- range $shardIndex := until (int $.Values.shardedCluster.shards) }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "mongodb.fullname" $ }}-shard-{{ $shardIndex }}-arbiter-headless
  namespace: {{ include "cloudpirates.namespace" $ }}
  labels:
    {{- include "mongodb.labels" $ | nindent 4 }}
    service-type: shard-arbiter
    shard-index: "{{ $shardIndex }}"
  {{- with (include "mongodb.annotations" $) }}
  annotations:
{{- . | indent 4 }}
  {{- end }}
spec:
  type: ClusterIP
  clusterIP: None
  publishNotReadyAddresses: true
  ports:
    - name: mongodb
      port: 27017
      targetPort: mongodb
      protocol: TCP
    {{- if $.Values.metrics.enabled }}
    - name: http-metrics
      port: 9216
      targetPort: http-metrics
      protocol: TCP
    {{- end }}
  selector:
    {{- include "mongodb.selectorLabels" $ | nindent 4 }}
    service-type: shard-arbiter
    shard-index: "{{ $shardIndex }}"
{{- end }}
{{- end }}
{{- end }}